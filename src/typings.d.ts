/**
 * Build-time constants replaced by the Angular CLI `define` option.
 * Defaults live in angular.json; `npm run build` passes the values from the environment.
 */
declare const CARTO_API_KEY: string;
