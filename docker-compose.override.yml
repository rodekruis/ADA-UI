services:
  ada-ui:
    volumes:
      - ./:/home/node/app
      - node-modules:/home/node/app/node_modules

  nginx:
    image: nginx
    volumes:
      - ./nginx/http.conf:/etc/nginx/conf.d/default.conf

volumes:
  node-modules:
